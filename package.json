{
  "name": "ce-assignment3-2",
  "version": "1.0.0",
  "description": "Node.js application with CI/CD pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "node test/app.test.js",
    "test:deploy": "echo 'Deployment tests passed' && exit 0",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "echo 'Build completed' && exit 0",
    "deploy": "echo 'Deployment script placeholder' && exit 0"
  },
  "keywords": ["nodejs", "cicd", "github-actions"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
